import { BuilbeDirective } from './builbe.directive';

describe('BuilbeDirective', () => {
  it('should create an instance', () => {
    const directive = new BuilbeDirective();
    expect(directive).toBeTruthy();
  });
});
