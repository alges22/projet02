import { QvRulesDirective } from './qv-rules.directive';

describe('QvRulesDirective', () => {
  it('should create an instance', () => {
    const directive = new QvRulesDirective();
    expect(directive).toBeTruthy();
  });
});
