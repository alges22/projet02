import { NeedValidationsDirective } from './need-validations.directive';

describe('NeedValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new NeedValidationsDirective();
    expect(directive).toBeTruthy();
  });
});
