import { QuickvDirective } from './quickv.directive';

describe('QuickvDirective', () => {
  it('should create an instance', () => {
    const directive = new QuickvDirective();
    expect(directive).toBeTruthy();
  });
});
