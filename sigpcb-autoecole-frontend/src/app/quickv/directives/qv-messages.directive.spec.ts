import { QvMessagesDirective } from './qv-messages.directive';

describe('QvMessagesDirective', () => {
  it('should create an instance', () => {
    const directive = new QvMessagesDirective();
    expect(directive).toBeTruthy();
  });
});
