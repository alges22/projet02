import { QvSubmitDirective } from './qv-submit.directive';

describe('QvSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new QvSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
