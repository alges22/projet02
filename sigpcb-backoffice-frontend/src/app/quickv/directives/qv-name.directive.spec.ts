import { QvNameDirective } from './qv-name.directive';

describe('QvNameDirective', () => {
  it('should create an instance', () => {
    const directive = new QvNameDirective();
    expect(directive).toBeTruthy();
  });
});
