import { PreventLinkDirective } from './prevent-link.directive';

describe('PreventLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventLinkDirective();
    expect(directive).toBeTruthy();
  });
});
