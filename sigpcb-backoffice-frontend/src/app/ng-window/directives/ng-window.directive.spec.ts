import { NgWindowDirective } from './ng-window.directive';

describe('NgWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new NgWindowDirective();
    expect(directive).toBeTruthy();
  });
});
